[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ripple-mcp"
version = "1.0.0"
description = "Field-level semantic impact analysis for Claude Code — see the ripple effect of any code change"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
keywords = ["mcp", "code-analysis", "impact-analysis", "ast", "claude", "ripple", "semantic"]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Topic :: Software Development :: Libraries",
]
dependencies = [
    "mcp>=1.0.0,<2.0.0",
    "anyio>=4.0.0,<5.0.0",
]

[project.scripts]
ripple-mcp = "field_impact_mcp.__main__:main"

[project.urls]
Homepage = "https://github.com/ybmyb/Ripple-Mcp"
Issues = "https://github.com/ybmyb/Ripple-Mcp/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/field_impact_mcp"]
